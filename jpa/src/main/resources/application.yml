spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always

  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true

    generate-ddl: false

    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: ERROR